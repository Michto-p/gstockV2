// public/js/moves.js
// Historique des mouvements (actuellement affiché dans Dashboard).
import { AppEvents } from "./core.js";

// Placeholder (garde le module vivant sans effets secondaires)
AppEvents.addEventListener("auth:signedIn", () => {});
